import { Typography } from "@mui/material";
import React from "react";
import { filterAnimeMangaData } from "../api/filterData";
import { useFetch } from "../api/useFetch";

const Game = ({ url }) => {
  return <Typography color="white">test</Typography>;
};

export default Game;
